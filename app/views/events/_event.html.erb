<div class="card card-block">
  <h4 class="card-title"><%= event.name %></h4>
  <p class="card-text"><%= event.description.truncate(100, separator: ' ') %></p>
  <%= link_to 'View', event_path(event), class: 'btn btn-primary' %>
</div>
