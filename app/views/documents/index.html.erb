<div class="main-div">
  <% if logged_in %>
    <% if User.find_by_id(session[:user_id]).admin? %>
      <%= link_to new_document_path, class: 'btn btn-info float-xs-right' do %>
        <i class="fa fa-plus"></i> Create a New Document
      <% end %>
    <% end %>
  <% end %>
  <h1>All Documents</h1>
  <div>
    <%= render @documents %>
  </div>
</div>
